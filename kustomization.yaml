apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - svc.yaml
  - namespace.yaml
  - statefulset.yaml
  - ingress.yaml
  - pvc.yaml
  - pool.yaml
  - storageclass.yaml

secretGenerator:
- name: validator-key 
  literals:
  - password=1f2d1e2e67df

